//! Library entrypoints for integration tests and reuse.
//
// This project is primarily a binary (`main.rs`), but exposing core modules as a
// library makes it easy to write integration tests (e.g. JSON parsing).

pub mod types;
